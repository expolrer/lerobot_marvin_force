# 当前工作区对应的模型短名称。
model_name: fcact

# 当前 Git 分支名称，用于确定环境命名。
branch_name: fcact

# Conda 创建和运行的环境名称。
environment_name: lerobot_marvin_fcact

# LeRobot 0.6.1 要求的 Python 主次版本。
python_version: "3.12"

# 仅安装训练、Marvin 硬件和数据审查绘图所需的可选依赖。
editable_extras: "[training,hardware,matplotlib-dep]"
